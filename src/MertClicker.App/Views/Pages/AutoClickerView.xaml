<UserControl x:Class="MertClicker.App.Views.Pages.AutoClickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="FieldLabelStyle">
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,0,16,8"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="480" HorizontalAlignment="Left">
            <TextBlock Text="Auto Clicker" Style="{StaticResource PageTitleStyle}"/>

            <TextBlock Text="Fare Tuşu" Style="{StaticResource FieldLabelStyle}"/>
            <ComboBox ItemsSource="{Binding MouseButtonOptions}" SelectedItem="{Binding SelectedButton}"/>

            <TextBlock Text="Tıklama Türü" Style="{StaticResource FieldLabelStyle}"/>
            <ComboBox ItemsSource="{Binding ClickTypeOptions}" SelectedItem="{Binding SelectedClickType}"/>

            <TextBlock Text="Tıklama Aralığı" Style="{StaticResource FieldLabelStyle}"/>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Margin="0" Text="{Binding IntervalValue, UpdateSourceTrigger=PropertyChanged}"/>
                <ComboBox Grid.Column="2" Margin="0" ItemsSource="{Binding IntervalUnitOptions}" SelectedItem="{Binding SelectedIntervalUnit}"/>
            </Grid>

            <TextBlock Text="Tekrar Modu" Style="{StaticResource FieldLabelStyle}"/>
            <ComboBox ItemsSource="{Binding RepeatModeOptions}" SelectedItem="{Binding SelectedRepeatMode}"/>

            <StackPanel Visibility="{Binding SelectedRepeatMode, Converter={StaticResource RepeatModeToVisibilityConverter}}">
                <TextBlock Text="Tekrar Sayısı" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox Text="{Binding RepeatCount, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <TextBlock Text="Konum" Style="{StaticResource FieldLabelStyle}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <RadioButton Content="Mevcut fare konumu" GroupName="Target" IsChecked="{Binding UseFixedPoint, Converter={StaticResource InverseBooleanConverter}}"/>
                <RadioButton Content="Sabit koordinat" GroupName="Target" IsChecked="{Binding UseFixedPoint}"/>
            </StackPanel>

            <StackPanel Visibility="{Binding UseFixedPoint, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,16">
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Margin="0" Text="{Binding FixedX, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBox Grid.Column="2" Margin="0" Text="{Binding FixedY, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
                <Button Content="Konumu Yakala (3 sn)" Command="{Binding CaptureCoordinateCommand}"
                        Padding="12,6" HorizontalAlignment="Left"/>
                <TextBlock Text="{Binding CaptureCountdownText}" Style="{StaticResource PageBodyStyle}" Margin="0,8,0,0"/>
            </StackPanel>

            <TextBlock Text="Başlangıç Gecikmesi (saniye)" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Text="{Binding StartDelaySeconds, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="{Binding HotkeyStatusText}" Style="{StaticResource PageBodyStyle}" Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,16">
                <Button Content="Başlat" Command="{Binding StartCommand}" Padding="16,8" Margin="0,0,12,0"
                        Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0"/>
                <Button Content="Duraklat" Command="{Binding PauseCommand}" Padding="16,8" Margin="0,0,12,0"/>
                <Button Content="Devam Et" Command="{Binding ResumeCommand}" Padding="16,8" Margin="0,0,12,0"/>
                <Button Content="Durdur" Command="{Binding StopCommand}" Padding="16,8"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource PageBodyStyle}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
