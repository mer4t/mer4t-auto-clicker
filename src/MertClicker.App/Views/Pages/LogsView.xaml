<UserControl x:Class="MertClicker.App.Views.Pages.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Kayıtlar" Style="{StaticResource PageTitleStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Yenile" Command="{Binding RefreshCommand}" Padding="16,8" Margin="0,0,12,0"
                    Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0"/>
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource PageBodyStyle}" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
            <ListBox ItemsSource="{Binding LogLines}" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="4,1"/>
                        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <TextBlock Grid.Row="3" Text="Yalnızca en güncel günün kayıt dosyası gösterilir."
                   Style="{StaticResource PageBodyStyle}" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
