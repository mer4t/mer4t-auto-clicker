<Application x:Class="MertClicker.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MertClicker.App.ViewModels.Pages"
             xmlns:views="clr-namespace:MertClicker.App.Views.Pages"
             xmlns:conv="clr-namespace:MertClicker.App.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Colors.xaml"/>
                <ResourceDictionary Source="Themes/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            <conv:RepeatModeToVisibilityConverter x:Key="RepeatModeToVisibilityConverter"/>

            <DataTemplate DataType="{x:Type vm:HomeViewModel}">
                <views:HomeView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AutoClickerViewModel}">
                <views:AutoClickerView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:MacroRecorderViewModel}">
                <views:MacroRecorderView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:MyMacrosViewModel}">
                <views:MyMacrosView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:LogsViewModel}">
                <views:LogsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AboutViewModel}">
                <views:AboutView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
