<UserControl x:Class="m4AutoClicker.App.Views.Pages.MacroRecorderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <HeaderedContentControl Header="Makro Kaydedici" Style="{StaticResource PageShellStyle}">
        <StackPanel HorizontalAlignment="Left">
            <TextBlock Text="Kayıt sırasında fareyle yaptığınız hareketler, tıklamalar ve tekerlek hareketleri kaydedilir."
                       Style="{StaticResource PageBodyStyle}" MaxWidth="620" Margin="{StaticResource SectionSpacing}"/>

            <!-- Kayıt durumu -->
            <Border Style="{StaticResource CardStyle}" Margin="{StaticResource SectionSpacing}">
                <StackPanel>
                    <Label Content="Kayıt Durumu" Style="{StaticResource SectionHeaderStyle}"/>
                    <TextBlock Text="{Binding HotkeyStatusText}" Style="{StaticResource PageBodyStyle}" Margin="0,0,0,6"/>
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource PageBodyStyle}" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,16"/>
                    <WrapPanel>
                        <Button Content="Kaydı Başlat" Command="{Binding StartRecordingCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="160" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Kaydı Durdur" Command="{Binding StopRecordingCommand}" Width="160" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Son kaydı oynat -->
            <Border Style="{StaticResource CardStyle}" Margin="{StaticResource SectionSpacing}">
                <StackPanel>
                    <Label Content="Son Kaydı Oynat" Style="{StaticResource SectionHeaderStyle}"/>
                    <WrapPanel>
                        <Button Content="Oynat" Command="{Binding PlayLastRecordingCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="140" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Duraklat" Command="{Binding PausePlaybackCommand}" Width="140" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Devam Et" Command="{Binding ResumePlaybackCommand}" Width="140" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Oynatmayı Durdur" Command="{Binding StopPlaybackCommand}" Width="150" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Kütüphaneye kaydet -->
            <Border Style="{StaticResource CardStyle}" Margin="{StaticResource SectionSpacing}">
                <StackPanel>
                    <Label Content="Son Kaydı Kütüphaneye Kaydet" Style="{StaticResource SectionHeaderStyle}"/>
                    <TextBlock Text="Makro Adı" Style="{StaticResource FieldLabelStyle}"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Text="{Binding LastRecordedMacroName, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
                        <Button Content="Kaydet" Command="{Binding SaveLastRecordingCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="Not: F8 / 'Son Kaydı Oynat', kaydetmeden de en son yapılan kaydı hızlıca test etmenizi sağlar. Kütüphanedeki kayıtlı bir makroyu F8 ile oynatmak için Makrolarım ekranından bir makro seçin."
                       Style="{StaticResource SecondaryTextStyle}" MaxWidth="620"/>
        </StackPanel>
    </HeaderedContentControl>
</UserControl>
