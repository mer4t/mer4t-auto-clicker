<UserControl x:Class="m4AutoClicker.App.Views.Pages.AutoClickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <HeaderedContentControl Header="Auto Clicker" Style="{StaticResource PageShellStyle}">
        <StackPanel HorizontalAlignment="Left">
            <WrapPanel>
                <!-- Temel tıklama ayarları -->
                <Border Style="{StaticResource CardStyle}" Width="340" Margin="0,0,24,24" VerticalAlignment="Top">
                    <StackPanel>
                        <Label Content="Tıklama Ayarları" Style="{StaticResource SectionHeaderStyle}"/>

                        <TextBlock Text="Fare Tuşu" Style="{StaticResource FieldLabelStyle}"/>
                        <ComboBox ItemsSource="{Binding MouseButtonOptions}" SelectedItem="{Binding SelectedButton}" Width="240"/>

                        <TextBlock Text="Tıklama Türü" Style="{StaticResource FieldLabelStyle}"/>
                        <ComboBox ItemsSource="{Binding ClickTypeOptions}" SelectedItem="{Binding SelectedClickType}" Width="240"/>

                        <TextBlock Text="Tıklama Aralığı" Style="{StaticResource FieldLabelStyle}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBox Width="140" Margin="0" Text="{Binding IntervalValue, UpdateSourceTrigger=PropertyChanged}"/>
                            <ComboBox Width="140" Margin="10,0,0,0" ItemsSource="{Binding IntervalUnitOptions}" SelectedItem="{Binding SelectedIntervalUnit}"/>
                        </StackPanel>

                        <TextBlock Text="Tekrar Modu" Style="{StaticResource FieldLabelStyle}"/>
                        <ComboBox ItemsSource="{Binding RepeatModeOptions}" SelectedItem="{Binding SelectedRepeatMode}" Width="240"/>

                        <StackPanel Visibility="{Binding SelectedRepeatMode, Converter={StaticResource RepeatModeToVisibilityConverter}}">
                            <TextBlock Text="Tekrar Sayısı" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding RepeatCount, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Konum ve başlangıç ayarları -->
                <Border Style="{StaticResource CardStyle}" Width="340" Margin="0,0,0,24" VerticalAlignment="Top">
                    <StackPanel>
                        <Label Content="Konum ve Başlangıç" Style="{StaticResource SectionHeaderStyle}"/>

                        <TextBlock Text="Konum" Style="{StaticResource FieldLabelStyle}"/>
                        <StackPanel Margin="0,0,0,8">
                            <RadioButton Content="Mevcut fare konumu" GroupName="Target" Margin="0,0,0,8"
                                         IsChecked="{Binding UseFixedPoint, Converter={StaticResource InverseBooleanConverter}}"/>
                            <RadioButton Content="Sabit koordinat" GroupName="Target" Margin="0,0,0,8"
                                         IsChecked="{Binding UseFixedPoint}"/>
                        </StackPanel>

                        <StackPanel Visibility="{Binding UseFixedPoint, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBox Width="130" Margin="0" Text="{Binding FixedX, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBox Width="130" Margin="10,0,0,0" Text="{Binding FixedY, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <Button Content="Konumu Yakala (3 sn)" Command="{Binding CaptureCoordinateCommand}" HorizontalAlignment="Left"/>
                            <TextBlock Text="{Binding CaptureCountdownText}" Style="{StaticResource SecondaryTextStyle}" Margin="0,8,0,0"/>
                        </StackPanel>

                        <TextBlock Text="Başlangıç Gecikmesi (saniye)" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Text="{Binding StartDelaySeconds, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0,0,0,16"/>

                        <TextBlock Text="{Binding HotkeyStatusText}" Style="{StaticResource SecondaryTextStyle}"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Label Content="Kontrol" Style="{StaticResource SectionHeaderStyle}"/>
                    <WrapPanel Margin="0,0,0,4">
                        <Button Content="Başlat" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Duraklat" Command="{Binding PauseCommand}" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Devam Et" Command="{Binding ResumeCommand}" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                        <Button Content="Durdur" Command="{Binding StopCommand}" Margin="{StaticResource ButtonGroupItemSpacing}"/>
                    </WrapPanel>
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource PageBodyStyle}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </HeaderedContentControl>
</UserControl>
