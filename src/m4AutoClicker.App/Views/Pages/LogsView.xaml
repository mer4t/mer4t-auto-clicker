<UserControl x:Class="m4AutoClicker.App.Views.Pages.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <HeaderedContentControl Header="Kayıtlar" Style="{StaticResource PageShellFixedStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <Button Content="Yenile" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource PageBodyStyle}" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0">
                <ListBox ItemsSource="{Binding LogLines}" Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Setter Property="Padding" Value="4,1"/>
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>

            <TextBlock Grid.Row="2" Text="Yalnızca en güncel günün kayıt dosyası gösterilir."
                       Style="{StaticResource SecondaryTextStyle}" Margin="0,10,0,0"/>
        </Grid>
    </HeaderedContentControl>
</UserControl>
